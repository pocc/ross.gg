{
  "name": "ross.gg",
  "private": true,
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --minify",
    "deploy": "npx wrangler deploy",
    "preview": "hugo --minify && npx wrangler dev"
  },
  "devDependencies": {
    "typescript": "^5",
    "wrangler": "^4",
    "@cloudflare/workers-types": "^4"
  }
}
